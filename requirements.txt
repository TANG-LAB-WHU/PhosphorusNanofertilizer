# NanoP LCA-TEA Framework Dependencies
# Primary source of truth: pyproject.toml
# Install: pip install -e ".[ai,viz,dev]"

# Core
numpy>=1.24.0
scipy>=1.10.0
pyyaml>=6.0
networkx>=3.2.0
python-dotenv>=1.0.0

# AI/ML - LightRAG (Knowledge Graph + RAG)
lightrag-hku>=1.0.0
google-genai>=1.0.0

# Data Processing
pandas>=2.0.0
PyMuPDF>=1.23.0

# PDF Parsing (Advanced) - Optional
mineru[all]  # MinerU support (GPU required)

# Development (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
